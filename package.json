{
  "name": "mind-racing",
  "version": "0.0.1",
  "description": "tbc",
  "repository": "https://github.com/danburonline/mind-racing.git",
  "author": "Daniel Burger <public@danielburger.online>",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.4.0"
  },
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write \"packages\"",
    "lint": "yarn workspaces run lint",
    "build:docs": "yarn workspaces run build:doc"
  },
  "lint-staged": {
    "*": "yarn format"
  }
}
