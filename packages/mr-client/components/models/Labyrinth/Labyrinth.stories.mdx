import { Loader, OrbitControls } from '@react-three/drei'
import { Canvas } from '@react-three/fiber'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs'
import { Suspense } from 'react'
import Lighting from '../scenes/MainGameScene/components/Lighting.tsx'
import Labyrinth from './Labyrinth'

<Meta title='Models/Labyrinth' />

# Labyrinth

This is the basic labyrinth component with a basic lighting setup.

<Preview>
  <Story name='Labyrinth'>
    <Canvas style={{ height: '100vh', backgroundColor: 'black' }}>
      <Suspense fallback={null}>
        <Labyrinth />
      </Suspense>
      <Lighting />
      <OrbitControls />
    </Canvas>
  </Story>
</Preview>

## Props

<Props of={Labyrinth} />
